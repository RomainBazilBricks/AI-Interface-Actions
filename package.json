{
  "name": "ai-interface-actions",
  "version": "1.0.0",
  "description": "Outil d'automatisation pour les plateformes IA via leur interface web",
  "scripts": {
    "dev": "source venv/bin/activate && uvicorn ai_interface_actions.api:app --host 0.0.0.0 --port 3000 --reload",
    "start": "source venv/bin/activate && uvicorn ai_interface_actions.api:app --host 0.0.0.0 --port 3000",
    "test": "source venv/bin/activate && python scripts/test.py",
    "test-visible": "source venv/bin/activate && python scripts/test_visible.py",
    "setup": "chmod +x scripts/install.sh && ./scripts/install.sh"
  },
  "keywords": ["ai", "automation", "fastapi", "browser", "playwright"],
  "author": "Romain",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
} 